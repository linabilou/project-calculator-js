const displayHistoryEl = document.querySelector('.display-history'); 
const displayFinalResultEl = document.querySelector('.display-finalResult');
const displayTemporaryResultEl = document.querySelector('.display-temporaryResult');
const numbersEl = document.querySelectorAll('.number');
const operationEl = document.querySelectorAll('.operation');
const equalEl = document.querySelector('.equal');
const clearEl = document.querySelector('.clear-all');
const lastEl = document.querySelectorAll('last-entity-clear');